# This file contains configuration overrides for local development.
source:
  postgres:
    serverName: localhost
  influx:
    serverName: localhost
  latencyts:
    ampicmp:
      location: ./data/latency-ts-i/ampicmp
    smokeping:
      location: ./data/latency-ts-i/smokeping
