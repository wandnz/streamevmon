package nz.net.wand.streamevmon

/** This package allows detectors to specify the parameters they accept, and
  * any relational restrictions between them. For example, you can specify that
  * parameter A can be any integer between 0 and 100, and that parameter B must
  * be less than half of parameter A's value.
  */
package object parameters {}
